SELECT * FROM Student;
SELECT TOP 50 * FROM Student;
-- Note: The syntax for limiting the number of rows returned may vary depending on the database system you are using. The above query is for SQL Server. For MySQL, you can use "LIMIT 50" instead of "TOP 50".
SELECT name FROM Student;
SELECT DISTINCT Amount FROM Orders;
